import UserInfo from '/app/ui/UserProfile/UserInfo';

export default function page(){

    // Connect to database
    // const profileData = getProfileData();

    return (
        < UserInfo />
    );
};